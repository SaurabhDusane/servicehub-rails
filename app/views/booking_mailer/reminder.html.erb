<h2>See you soon!</h2>
<p>Just a reminder: your appointment with <strong><%= @booking.provider_profile.business_name %></strong> is coming up on <%= @booking.start_time.strftime("%A, %B %d at %l:%M %p") %>.</p>
<a class="button" href="<%= customer_booking_url(@booking, host: ENV.fetch('MAILER_HOST', 'localhost:3000')) %>">View booking</a>
